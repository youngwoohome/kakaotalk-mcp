#!/usr/bin/env node
'use strict';

const path = require('path');

process.chdir(path.resolve(__dirname, '..'));

if (process.argv[2] === 'serve') {
  process.argv.splice(2, 1);
  require('../src/mcp/server.js');
} else {
  require('../src/cli/mcp.js');
}
